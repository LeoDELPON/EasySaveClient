<Window x:Class="EasySaveClient.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:EasySaveClient"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="1" Margin="10 0" FontSize="28">
            Cesi EasySave client
        </TextBlock>
        <Button x:Name="ConnectToServer" Grid.Row="2" Margin="10" Padding="10" Click="ConnectToServer_Click">Se connecter</Button>
        <Button x:Name="ExecuteWork" Grid.Row="3" Margin="10" Padding="10" >Exécuter un travail</Button>
        <ListBox Name="WorkList" Grid.Row="4"/>

    </Grid>
</Window>
